[제이쿼리/jQuery] method 메소드 
https://www.codingfactory.net/10292

.css()로 선택한 요소의 css 속성값을 가져오거나 style 속성을 추가
$( "h1" ).css( "color" );
$( "h1" ).css( "color", "green" );



.add()는 어떤 요소를 추가로 선택
.add( selector )
li 요소를 선택하고, 추가로 p 요소를 선택한 후 색을 빨간색으로 
   $( 'li' ).add( 'p' ).css( 'color', 'red' );
   
   
   
   
   .addBack( [selector ] )
   $( 'ul' ).find( 'li' ).addBack()=> ul의 하위 요소 중 li를 선택하고, 추가적으로 처음 선택했던 ul을 선택
  .addBack()은 현재 선택한 요소와 함께 이전에 선택한 요소도 선택
  
  
  
  
.addClass()로 선택한 요소에 클래스 값을 추가할 수 있습니다.  
  .addClass( className )
  $( 'h1' ).addClass( 'ab cd ef' );
  띄어쓰기로 구분하여 여러 개의 값을 추가할 수 있습니다.
  
  
  
  
  .after()는 선택한 요소 뒤에 새 요소를 추가하거나, 다른 곳에 있는 요소를 이동
  .after( content [, content ] )
  $( 'h1' ).after( '<p>Hello</p>' );=> h1 요소 뒤에 Hello를 내용으로 갖는 p 요소를 추가
  $( 'h1.a' ).after( $( 'p.b' ) ); = > 클래스 값으로 a를 갖는 h1 요소 뒤에 클래스 값으로 b를 갖는 p 요소를 이동
  
  
  

   attr : 속성을 조회하거나 변경할수 있는 메소드 
 요소(element)의 속성(attribute)의 값을 가져오거나 속성을 추가  
   .attr( attributeName )
   $( 'div' ).attr( 'class' ); => div 요소의 class 속성의 값을 가져옵니다.
   
.removeAttr()은 선택한 요소의 특정 속성을 제거   
   
   
.load() : 다른 문서 등에서 내용을 가져와 현재 문서에 나타냄 
	문법 : .load( url [, data ] [, complete ] )
$( 'div' ).load( 'a.html p' ); =>  a.html의 p 요소를 가져와 div 요소 안에 넣기 
 $( '#xy' ).load( 'load-02.html #ab' ); => load-02.html에서 id 값이 ab인 요소를 가져와서, 현재 문서의 id 값이 xy인 요소 안에 넣습




.append()는 선택한 요소의 내용의 끝에 콘텐트를 추가
.append( content [, content ] )
$( 'p' ).append( ' 123' ); = > <p>Lorem Ipsum Dolor 123</p> 로 출력 




.appendTo() : 선택한 요소를 다른 요소 안에 넣습니다.
	문법 : .appendTo( target )
	ex : $( 'p' ).appendTo( 'blockquote' ); =>  p 요소를 blockquote 요소 안으로 이동
		 $( 'span.abc' ).appendTo( 'h1' ); => abc를 클래스 값으로 가지는 span 요소를 h1 요소 안으로 이동
		
		 
		 
.removeClass() : 선택한 요소에서 클래스 값을 제거
	문법 : .removeClass( className )
	ex : $( 'h1' ).removeClass( 'abc' );
	띄어쓰기로 구분하여 여러 개의 값을 제거
		$( 'h1' ).removeClass( 'ab cd ef' );



<before / insertBefore
.insertBefore(): 내용을 선택한 요소 앞에 추가 
.before() : 선택한 요소 앞에 새 요소를 추가하거나, 다른 곳에 있는 요소를 이동 
.before() 와 .insertBefore() 함수는 동일한 기능을 합니다. 중요한 차이점은 내용과 대상의 위치 차이
	$(".three").before("<li>앞에삽입</li>");
	$("<li>뒤에 삽입</li>").insertBefore(".three");
.before()는 이 함수 앞에 표현된 선택자이고 그 앞에 내용이 삽입됩니다. 반면 .insertBefore()은 함수 앞에 내용이 함수의 인자로 주어진 타겟 요소 앞에 삽입


.has()로 특정 요소를 가지고 있는 요소를 선택
.has( selector )
 $("h2:has(strong)").addClass("tit");=> h2태그중에 하위 요소인 strong태그를 포함하는 <h2>에 class="tit"속성 추가 

.hide()는 선택한 요소를 사라지게 합니다.
.hide(사라지는데 걸리는 시간  [, 사라지는방식(swing, linear ] [, 사라진 다음 실행할 함수 ] )




.click()은 선택한 요소를 클릭했을 때 특정 작업을 수행할 수 있게 하는 속성

button 요소를 클릭했을 때 함수를 실행시키고 싶으면
$( 'button' ).click( function() {
  // function
} );




